name: frontend
description: "A well-structured and functional Flutter project."

# Consider publishing if your project is reusable and has value for others
# publish_to: 'none'  # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.8  # Update to latest


  flutter_localizations:
    sdk: flutter
  get: ^4.6.6
  shared_preferences: ^2.3.2
  intl: ^0.19.0
  intl_utils: ^2.8.7

  # Additional dependencies (replace with your specific needs)
  # - socket_io_client: ^2.0.0  # For Socket.IO integration
  # ...

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1  # Update to latest (more focused linting)

flutter:
  generate: true
  uses-material-design: true

assets:
  - assets/images/
  - assets/svg/
  # Add other asset paths here

# Consider adding fonts if needed (example)
# fonts:
#   - family: Schyler
#     fonts:
#       - asset: fonts/Schyler-Regular.ttf
#       - asset: fonts/Schyler-Italic.ttf
#         style: italic

# Localization setup (optional, add if needed)